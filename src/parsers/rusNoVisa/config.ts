import type { ParserConfig } from '../parsers.types';


const url = 'https://www.tourister.ru/publications/359';
const parserName = 'RusNoVisa';

export const rusNoVisaConfig: ParserConfig = {
  url,
  parserName,
};
